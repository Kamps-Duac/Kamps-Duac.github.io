'use strict';

describe('projectGuac.projectView module', function() {

  beforeEach(module('projectGuac.projectListView'));

  describe('projectListView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var projectListCtrl = $controller('ProjectListCtrl');
      expect(projectListCtrl).toBeDefined();
    }));

  });
});